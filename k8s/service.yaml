apiVersion: v1
kind: Service
metadata:
  name: self-healing-api-svc
  namespace: self-healing
spec:
  selector:
    app: self-healing-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: LoadBalancer
